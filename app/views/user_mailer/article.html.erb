<%= render 'header' %>
<div class="mail-content article-shadow">
  <div class="logo-title">
    <img src="https://res.cloudinary.com/dmbf8fog4/image/upload/c_scale,w_100/v1555969918/JSA%20Triathlon/logo.jpg" alt="">
    <p class="jsa-title">
      Angoulême Triathlon
    </p>
  </div>

  <p>Bonjour <%= @user.firstname %>, </p>
  <p>Un nouvel article vient d'être publié sur notre site dans la rubrique <%= @article.article_type.name.downcase %>.</p>
  <span class="bold-mail">
    <%= @article.name %>
  </span>
  <p>
    Cet événement aura lieu le
    <span class="bold-mail">
      <%= l @article.date, format: '%A' %>
      <%= l @article.date, format: '%d' %>
      <%= l @article.date, format: '%B'%>
      <%= l @article.date, format: '%Y'%>
    </span>
    à
    <span class="bold-mail">
      <%= @article.location %>
    </span>
    à partir de
    <span class="bold-mail">
      <%= @article.start_time.strftime('%H:%M') %>
    </span>
  </p>
  <div class="mail-picture" style="background-image: linear-gradient(rgba(0,0,0,0.1), rgba(0,0,0,0.1)), url('<%= cl_image_path(@article.photo) %>') ">
  </div>
  <p>
    <%= raw @article.content %>
  </p>
  <div class="article-content-link">
    <a href=<%= 'https://jsa-triathlon.herokuapp.com/articles/' + "#{@article.id.to_s}" %> >
        <div class="article-link">
        Voir l'article
        <i class="fas fa-long-arrow-alt-right"></i>
      </div>
    </a>
  </div>


  <p>Bonne journée</p>
  <p>
    <%= @article.user.firstname %>
    (<%= @article.user.profil %>)
  </p>
  <p>
    <i class="fas fa-mobile-alt"></i>
    <%= @article.user.mobil_phone %>
  </p>
  <p>
    <i class="far fa-envelope"></i>
    <%= @article.user.email %>
  </p>
</div>

<%= render 'footer' %>
