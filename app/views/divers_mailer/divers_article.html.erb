<%= render 'user_mailer/header' %>
<p>Bonjour à toutes et à tous, </p>
<p>Un nouvel article vient d'être publié sur le site :
  <span class="bold-mail">
    <%= @article.name %>
  </span>
</p>

  <div class="mail-picture" style="background-image: linear-gradient(rgba(0,0,0,0.1), rgba(0,0,0,0.1)), url('<%= cl_image_path(@article.photo) %>') ">
  </div>
  <p>
    <%= raw @article.content %>
  </p>
  <div class="article-content-link">
    <a href=<%= 'https://www.angouleme-triathlon.fr/articles/' + "#{@article.id.to_s}" %> >
        <div class="article-link">
        Voir l'article
        <i class="fas fa-long-arrow-alt-right"></i>
      </div>
    </a>
  </div>


  <p>Bonne journée</p>
  <p>
    <%= @article.authors.last.user.firstname %>
    (<%= @article.authors.last.user.profil %>)
  </p>
  <p>
    <i class="fas fa-mobile-alt"></i>
    <%= @article.authors.last.user.mobil_phone %>
  </p>
  <p>
    <i class="far fa-envelope"></i>
    <%= @article.authors.last.user.email %>
  </p>
<%= render 'user_mailer/footer' %>
