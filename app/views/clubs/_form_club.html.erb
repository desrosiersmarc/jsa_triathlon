<%= simple_form_for @club do |f| %>
  <%= f.text_area :content, class: "ckeditor", label: "Contenu de l'article" %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit 'Enregistrer', class: "btn btn-danger btn-block" %>
<% end %>
