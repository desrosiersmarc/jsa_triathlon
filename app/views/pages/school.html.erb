<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h3>L'actualité de l'école de triathlon</h3>
    </div>
    <% @schools.each do |school| %>
      <div class="col-xs-12 col-sm-4">
          <%= render 'homepage_card_template', {article: school} %>
      </div>
    <% end %>
  </div>
</div>
